# Maintainer: Carlos David Isturiz <light.log at proton dot me>

pkgname=warp-control
pkgver=2.0.0
pkgrel=1
source_commit='e1b0ef24a367de0c1eea33e850c10d1c5cf02e55'
pkgdesc='GTK desktop control panel for Cloudflare WARP (experimental on Arch)'
arch=('any')
url='https://github.com/Light-log/warp-control-fedora'
license=('MIT')
depends=(
  'python'
  'python-gobject'
  'gtk3'
  'libayatana-appindicator'
  'python-idna'
)
makedepends=(
  'git'
  'python-build'
  'python-installer'
  'python-setuptools'
  'python-wheel'
)
checkdepends=('python-pytest')

# The VCS checksum is skipped only because the source is pinned to the complete,
# immutable commit above. Arch users must obtain warp-cli separately from a
# community source; no privileged installation helper is bundled.
source=("$pkgname::git+https://github.com/Light-log/warp-control-fedora.git#commit=$source_commit")
sha256sums=('SKIP')

build() {
  cd "$srcdir/$pkgname"
  python -m build --wheel --no-isolation
}

check() {
  cd "$srcdir/$pkgname"
  python -m pytest -m 'not ui'
}

package() {
  cd "$srcdir/$pkgname"
  python -m installer --destdir="$pkgdir" dist/*.whl

  install -Dm644 data/com.robler.warpcontrol.desktop \
    "$pkgdir/usr/share/applications/com.robler.warpcontrol.desktop"
  install -Dm644 data/com.robler.warpcontrol.metainfo.xml \
    "$pkgdir/usr/share/metainfo/com.robler.warpcontrol.metainfo.xml"
  install -Dm644 data/icons/com.robler.warpcontrol.svg \
    "$pkgdir/usr/share/icons/hicolor/scalable/apps/com.robler.warpcontrol.svg"
  install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
}
